<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <%- include('../partials/css') %>
    <style>
        .badge {
            padding: 5px 20px;
            border-radius: 30px;
            background-color: #3D7A9C;
            font-size:16px;
        }
        .details {
            border-radius: 8px;
            background-color: #F5F5F5;
            padding: 8px 10px;
        }
        .not-approved {
            color: rgb(241, 76, 76);
        }
        .approved {
            color: green;
        }
    </style>
</head>

<body>

    <!-- Include the left navigation -->
    <%- include('../partials/adminNavbar') %>
        <div class="container mt-4">
            <div class="container mt-3 mb-3">
                <div class="row justify-content-between align-items-center">
                    <div class="col-auto">
                        <a href="/admin/dashboard">
                            <img src="/images/Logowithname.png" alt="Logo" class="img-fluid logo">
                        </a>
                    </div>
                    <div class="col-auto text-right">
                        <span class="blockquote"
                            style="font-size: 14px;margin-right: 10px; padding-right:10px; border-right: 1px solid grey;">
                            <%= email%>
                        </span>
                        <a href="/" class="blockquote text-decoration-none text-secondary"
                            style="font-size: 14px;">Logout</a>
                    </div>
                </div>
                <div class="row justify-content-between align-items-center" style="margin-top: 100px;">                    
                    <%- include('../partials/userProfile') %>                                        
                </div>
            </div>

        </div>

        <!-- Bootstrap Confirmation Modal -->
        <div class="modal fade" id="declineModal" tabindex="-1" aria-labelledby="declineModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="declineModalLabel">Decline User</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        Are you sure you want to decline and delete this user?
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-danger" id="confirmDecline">Decline</button>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../partials/javascript') %>
        <%- include('../partials/approveUser') %>
</body>

</html>