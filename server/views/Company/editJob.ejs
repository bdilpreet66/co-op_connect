<!DOCTYPE html>
<html>
<head>
    <title>Edit Job</title>
    <!-- Add your CSS links here -->    
    <%- include('../partials/css') %>
</head>
<body>
    <!-- Include the left navigation -->
    <%- include('../partials/companyLeftNavigation') %>
    <div class="right-content">
        <h1>Edit Job</h1>
        <form action="/company/job/edit/<%= job._id %>" method="POST">        
            <div>
                <label for="jobTitle">Job Title*:</label>
                <input type="text" id="jobTitle" name="jobTitle" value="<%= job.jobTitle %>" required>
            </div>            
            <div>
                <label for="workSetup">Work Setup*:</label>
                <select id="workSetup" name="workSetup">
                    <option value="Hybrid" <%= job.workSetup === 'Hybrid' ? 'selected' : '' %>>Hybrid</option>
                    <option value="Remote" <%= job.workSetup === 'Remote' ? 'selected' : '' %>>Remote</option>
                    <option value="In-Office" <%= job.workSetup === 'In-Office' ? 'selected' : '' %>>In-Office</option>
                </select>
            </div>
            <div>
                <label for="jobLocation">Job Location*:</label>
                <input type="text" id="jobLocation" name="jobLocation" value="<%= job.jobLocation %>" required>
            </div>
            <div>
                <label for="jobDescription">Job Description*:</label>
                <textarea id="jobDescription" name="jobDescription" rows="15" required><%= job.jobDescription %></textarea>
            </div>
            <div>
                <label for="status">Status:</label>
                <select id="status" name="status">
                    <option value="open" <%= job.status === 'open' ? 'selected' : '' %>>Open</option>
                    <option value="closed" <%= job.status === 'closed' ? 'selected' : '' %>>Closed</option>
                </select>
            </div>
            <div class="button-container">
                <a type="button" class="btn btn-secondary" href="/company/jobs/">Cancel</a>
                <button type="submit" class="btn btn-primary">Update Job</button>        
            </div>
        </form>
    </div>
    <!-- Add your JS scripts here -->
    <%- include('../partials/javascript') %>
</body>
</html>
